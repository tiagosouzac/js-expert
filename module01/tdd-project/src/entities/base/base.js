class Base {
  /**
   * @param {object} param0
   * @param {string} param0.id
   * @param {string} param0.name
   */
  constructor({ id, name }) {
    this.id = id
    this.name = name
  }
}

module.exports = Base
